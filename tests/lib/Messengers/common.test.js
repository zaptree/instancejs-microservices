'use strict';

var assert = require('chai').assert;

describe('Messengers', function(){

	it.skip('should return a common response when a 404 is returned with all messenger types', function(){
		assert(false, 'not implemented');
	});

	it.skip('should return a common response when an error occurs with all messenger types', function(){
		assert(false, 'not implemented');
	});

});
